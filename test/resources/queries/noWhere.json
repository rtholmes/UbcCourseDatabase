{
	"title": "having a missing WHERE passes on data as is without filtering",
	"input": {
		"WHERE": {

		},
		"OPTIONS": {
			"COLUMNS": [
				"courses_dept",
				"courses_id",
				"courses_avg"
			],
			"ORDER": "courses_avg"
		}
	},
	"errorExpected": true,
	"expected": "ResultTooLargeError"
}
